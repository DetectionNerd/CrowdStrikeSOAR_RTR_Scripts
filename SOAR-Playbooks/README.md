| Queries |
|-----------|
| [`1. Lost/Stolen Device: automate cached credential removal and BitLocker key revocation`](./Lost-Stolen-Device) |
| [`2. Email Alert: send a NG-SIEM detection search query based alert in a tabular format`](./EmailAlerts) |
